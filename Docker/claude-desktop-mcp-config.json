{
  "mcpServers": {
    "mcp-swarm-intelligence": {
      "command": "docker",
      "args": [
        "run", "--rm", "-i",
        "-v", "/tmp/mcp-swarm-data:/app/data",
        "--name", "mcp-swarm-claude",
        "mcp-swarm-server:latest"
      ]
    }
  }
}